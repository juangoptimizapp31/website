const Home = () => {
  return (
    <>
      <section id="inicio">
        <Hero />
      </section>

      <section id="soluciones">
        <ServicesCatalog />
      </section>

      <section id="servicios">
        <Automation />
      </section>

      <section id="confianza">
        <Guarantee />
      </section>

      <section id="contacto">
        <Contact />
      </section>
    </>
  );
};

export default Home;
